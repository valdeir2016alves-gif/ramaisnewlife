# dependencies
node_modules
client/node_modules
server/node_modules

# build output
client/dist
server/public

# local runtime data (production data lives in the Docker named volume)
/data
*.tsbuildinfo

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
